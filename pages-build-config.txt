# Cloudflare Pages Build Configuration

# Build Settings
# Build command: cd frontend && npm install && npm run build
# Build output directory: frontend/dist
# Root directory: /

# Environment Variables
# NODE_VERSION: 18

# PWA Headers will be automatically applied
